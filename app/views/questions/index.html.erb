<%= form_tag search_questions_path, :method => :get do%>
	<p>
			<%= label_tag :query, t("form.labels.search") %>
			<%= search_field_tag :query %>
			<%= submit_tag t("form.buttons.search") %>
	</p>	
<% end %>

<h1>Últimas perguntas</h1>

<ul>
	<li>
		<%= link_to t("filter.all"), root_path %>

	<li>
		<%= link_to t("filter.unanswered"), questions_path(:filter => QuestionFilter::UNANSWERED) %>
	</li>
</ul>

<% content_for :paginate do %>
	<%= paginate @questions %>
<% end %>

<%= yield :paginate %>

<% iterate @questions do |question| %>
	<%= render question %>
<% end %>

<%= yield :paginate %>